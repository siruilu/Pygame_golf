import os, sys
import pygame
import math
from pygame import locals

class GameSpace:
	def main(self):
		self.ball = Ball(self)
		self.course
		self.player1 = Player(self)
		self.player2 = Player(self)

if __name__ == "__main__":
    GameSpace()
